const add = (m, n) => {
    return m + n;
}
const sub = (m, n) => {
    return m - n;
}
const multi = (m, n) => {
    return m * n;
}
const divide = (m, n) => {
    return m / n;
}

module.exports = { add, sub, multi, divide };

